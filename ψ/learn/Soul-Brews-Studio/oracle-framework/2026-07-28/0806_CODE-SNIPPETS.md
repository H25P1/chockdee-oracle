# oracle-framework — Code Snippets

**Correction (2026-07-28)**: an earlier draft of this file pulled real code from a
*different* repo (`arra-oracle-skills-cli`'s `skills/recap/SKILL.md` etc.) and
presented it as if it lived in `oracle-framework`. It doesn't — this repo's own
directory listing confirms it.

## What's actually in this repo

```
oracle-framework/
├── .git/
└── README.md
```

That's it — a single `README.md`. There is no `skills/`, `agents/`, `.claude/`,
or source code of any kind in `Soul-Brews-Studio/oracle-framework`. This matches
what the Architecture exploration ([0806_ARCHITECTURE.md](0806_ARCHITECTURE.md))
found: the repo is pure philosophy documentation, not a reference implementation.

If you want to see the philosophy's principles *implemented* as actual skills,
agents, and a `.claude/` directory, that lives in
[`opensource-nat-brain-oracle`](../../opensource-nat-brain-oracle/opensource-nat-brain-oracle.md)
(learned in an earlier session) or in
[`arra-oracle-skills-cli`](../../arra-oracle-skills-cli/arra-oracle-skills-cli.md)
(learned in this session) — not here.
